# macOS
.DS_Store

# Processing IDE export builds
applet
application.linux-arm64
application.linux-armv6hf
application.linux32
application.linux64
application.windows32
application.windows64
application.macosx
out

# Artwork output — generated during testing, can get large fast.
# Rename this line if a particular program uses a different output folder name.
x - output/**

# Local data reference — stores the filepath to the user's selected data folder,
# never the data itself, but still shouldn't be committed.
data/preciousPrefs.txt
